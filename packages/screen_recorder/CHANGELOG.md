## 0.0.4

- Scaffold the independent `screen_recorder` package.
- Add the programmatic core API with an in-memory fake backend.
- Support Recording Device discovery, Recording Session start/stop/discard,
  output naming rules, result metadata, and stable error codes.
