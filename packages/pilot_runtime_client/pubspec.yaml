name: pilot_runtime_client
description: Dart VM Service client for Flutter Pilot runtime targets.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
