# 0.0.2 Real mcp_flutter Finder Integration Issues

Parent: https://github.com/drown0315/flutter_pilot/issues/36

This issue set narrows the parent issue to the current Runtime Adapter
contract: real integration coverage for `byText`, `byType`, and combined
Finder constraints. `byKey` is explicitly out of the current Scenario DSL
because `mcp_flutter` does not support stable key-based Finder matching at this
stage.

Progress: Complete by code reality. The repository contains the calibration
note, supported Finder smoke Scenario, CI-friendly smoke verifier, and optional
real `mcp_flutter` smoke workflow.

## 1. Calibrate real Finder support and document byKey boundary

GitHub: https://github.com/drown0315/flutter_pilot/issues/44

Type: AFK

Status: Complete

## Parent

https://github.com/drown0315/flutter_pilot/issues/36

## What to build

Record the real `mcp_flutter` Finder support boundary for the smoke app and
make the current contract visible in project documentation. The calibration
should confirm that the real integration path covers visible text, semantic
node type, and combined Finder constraints, while `byKey` remains outside the
current Scenario DSL.

## Acceptance criteria

- [x] A calibration note records the real Finder capabilities used by the smoke
      integration path.
- [x] Documentation says `byKey` is not supported in the current Scenario DSL
      because `mcp_flutter` does not currently provide stable key matching.
- [x] The parent issue scope is translated into current-contract coverage for
      `byText`, `byType`, and combined Finder constraints.
- [x] Existing Scenario YAML docs continue to advertise only supported Finder
      fields.

## Blocked by

None - can start immediately

## 2. Add real Finder integration Scenario for supported Finders

GitHub: https://github.com/drown0315/flutter_pilot/issues/45

Type: AFK

Status: Complete

## Parent

https://github.com/drown0315/flutter_pilot/issues/36

## What to build

Add a real smoke Scenario path that exercises the supported Finder mappings
through the real `McpFlutterRuntimeAdapter`. The Scenario should be runnable
against the smoke Flutter app and prove that visible text, semantic node type,
and combined Finder constraints map through Flutter Pilot, `mcp_flutter`, and
the real Runtime Target.

## Acceptance criteria

- [x] The smoke app exposes stable visible text and semantic roles for the
      Finder integration path.
- [x] A real Scenario exercises `byText`, `byType`, and at least one combined
      Finder constraint.
- [x] The integration path uses the real `McpFlutterRuntimeAdapter`, not a fake
      Runtime Adapter.
- [x] The Scenario includes a capture checkpoint that requests screenshot,
      Snapshot, and Logs artifacts.

## Blocked by

- Calibrate real Finder support and document byKey boundary

## 3. Make the real smoke verifier CI-friendly

GitHub: https://github.com/drown0315/flutter_pilot/issues/46

Type: AFK

Status: Complete

## Parent

https://github.com/drown0315/flutter_pilot/issues/36

## What to build

Upgrade the real smoke verification script so it can be used by both humans and
CI jobs. The verifier should run Flutter Pilot against an already-running
Runtime Target, locate the produced run report, and fail with clear exit codes
when the Scenario status, Step status, or required artifacts are wrong.

## Acceptance criteria

- [x] The verifier accepts a VM service URI and runs the real Finder integration
      Scenario.
- [x] The verifier reads the produced `run_report.json` and checks that the run
      status is `passed`.
- [x] The verifier checks that all Finder Steps passed.
- [x] The verifier checks that screenshot, Snapshot, and Logs artifacts are
      listed and exist on disk.
- [x] The verifier returns non-zero exit codes for failed runtime validation,
      failed Scenario runs, missing reports, failed Steps, or missing artifacts.

## Blocked by

- Add real Finder integration Scenario for supported Finders

## 4. Add optional CI workflow for real mcp_flutter integration

GitHub: https://github.com/drown0315/flutter_pilot/issues/47

Type: AFK

Status: Complete

## Parent

https://github.com/drown0315/flutter_pilot/issues/36

## What to build

Add an optional CI workflow that can automatically run the real
`mcp_flutter` Finder integration path without adding it to the default
`dart test` suite. The workflow should start the smoke Flutter app, obtain or
pass the VM service URI, run the smoke verifier, and archive the generated run
artifacts for inspection.

## Acceptance criteria

- [x] The workflow is opt-in and does not run as part of the default unit test
      command.
- [x] The workflow starts the smoke Flutter app in a way that exposes a VM
      service URI to the verifier.
- [x] The workflow runs the CI-friendly smoke verifier against the real Runtime
      Target.
- [x] The workflow archives the `.runs` output or the produced run directory as
      CI artifacts.
- [x] Documentation explains how the CI workflow relates to the manual smoke
      verification command.

## Blocked by

- Make the real smoke verifier CI-friendly
