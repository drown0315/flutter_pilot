# 0.1.1 pilot_runtime Issues

Parent PRD: https://github.com/drown0315/flutter_pilot/issues/110

Publishing status: skipped by user request; local issue breakdown only.

## 1. Scaffold pilot_runtime with protocol handshake

Status: completed in `91f9d21 feat(runtime): scaffold pilot runtime handshake`.

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Create the first usable `pilot_runtime` package slice: a Flutter package with a public barrel, an app-side `PilotRuntimeBinding`, a VM Service client entry point, protocol types, and initialization behavior. The slice should prove that Flutter Pilot can distinguish a valid debug Runtime Target with `PilotRuntimeBinding` from a target that is missing the hook, missing required capabilities, or running an incompatible protocol version.

## Acceptance criteria

- [x] `pilot_runtime` exists as an independent Flutter package with public app-side hook and client APIs.
- [x] `PilotRuntimeBinding.ensureInitialized()` can register a debug runtime handshake and is a no-op outside debug mode.
- [x] The client validates protocol version 1 and required capabilities through a handshake.
- [x] Missing hook, missing capability, and protocol mismatch produce clear initialization failures.
- [x] Unit tests cover successful handshake and each initialization failure path without requiring a live Flutter app.

Verification:

- `flutter analyze` from `packages/pilot_runtime`
- `flutter test` from `packages/pilot_runtime`
- `dart analyze`
- `dart test`

## Blocked by

None - can start immediately

## 2. Capture normalized Widget Tree through pilot_runtime

Status: completed in working tree.

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Implement the first diagnostic capability in `pilot_runtime`: capture a normalized Inspector Summary Widget Tree from a Debug Runtime Target. The client should set the Target App Package project root, request the Flutter Inspector summary tree with previews and without full details, normalize the response into Flutter Pilot's Widget Tree v1 shape, and fail clearly when required setup or response shape is invalid.

## Acceptance criteria

- [x] `captureWidgetTree()` sets Inspector pub root directories using the runtime target project root.
- [x] The client requests the root widget tree as a summary tree with previews and without full details.
- [x] The returned JSON includes `schema`, `source`, and a normalized root node.
- [x] Nodes normalize description, widget type, Inspector value id as `inspectorValueId`, text preview, local project marker, and children.
- [x] The normalizer does not parse keys from Inspector description strings.
- [x] Tests cover valid normalization, optional missing fields, invalid required shape, and Inspector call failures.

Verification:

- `flutter analyze` from `packages/pilot_runtime`
- `flutter test` from `packages/pilot_runtime`
- `dart analyze`
- `dart test`

## Blocked by

- 1. Scaffold pilot_runtime with protocol handshake

## 3. Expose PilotRuntimeAdapter behind hidden runtime switch

Status: completed in `7261c6c feat(runtime): expose pilot runtime adapter`.

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Wire Flutter Pilot to the new runtime without replacing the default bridge. Add a `PilotRuntimeAdapter` selected only through a hidden environment switch, keep `McpFlutterRuntimeAdapter` as the default, and prove an end-to-end initialize plus Widget Tree capture path through Flutter Pilot's runner-facing Runtime Adapter contract.

## Acceptance criteria

- [x] Flutter Pilot can select `PilotRuntimeAdapter` through a hidden environment switch.
- [x] The default runtime path still uses `McpFlutterRuntimeAdapter`.
- [x] Invalid hidden runtime switch values fail clearly without appearing in public CLI help.
- [x] `PilotRuntimeAdapter.initialize()` maps missing hook and protocol failures to run-level initialization failures.
- [x] `PilotRuntimeAdapter.captureWidgetTree()` returns Flutter Pilot `WidgetTreeCapture` data from `pilot_runtime`.
- [x] Tests cover default adapter selection, hidden switch selection, invalid switch values, and initialization failure behavior.

Verification:

- `dart format .`
- `dart analyze`
- `dart test`
- `flutter analyze` from `packages/pilot_runtime`
- `flutter test` from `packages/pilot_runtime`

## Blocked by

- 1. Scaffold pilot_runtime with protocol handshake
- 2. Capture normalized Widget Tree through pilot_runtime

## 4. Migrate structured UI capture from snapshot to widgetTree

Status: completed in `cd6882f feat(scenario)!: migrate capture defaults to Widget Tree`.

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Move Flutter Pilot's structured UI capture language from Snapshot to Widget Tree for the `pilot_runtime` path. Scenario YAML should use `widgetTree`, `snapshot` should no longer be accepted, default capture and failure diagnostics should include Widget Tree, and run artifacts should write Widget Tree JSON with the new artifact type and filename suffix.

## Acceptance criteria

- [x] Scenario capture YAML rejects `snapshot` and accepts `widgetTree`.
- [x] `capture: {}` defaults to Screenshot, Widget Tree, and Logs.
- [x] Automatic failure diagnostics request Widget Tree by default.
- [x] Widget Tree artifacts are written with a `widget_tree` JSON filename suffix.
- [x] Run reports use artifact type `widgetTree`.
- [x] `--print widget-tree` remains supported and `--print snapshot` is rejected with a clear message.
- [x] Parser, runner, artifact store, report, and CLI tests cover the new behavior.

Verification:

- `dart format .`
- `dart analyze`
- `dart test`

## Blocked by

- 3. Expose PilotRuntimeAdapter behind hidden runtime switch

## 5. Implement visible Finder resolution for byText and semantic byType

Status: completed in working tree.

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Implement the first app-side Finder resolution path in `pilot_runtime`. The runtime should resolve visible Runtime Target matches for exact user-visible text and Semantic Node Type, exclude targets that are clearly not visible, apply AND semantics, return opaque Runtime Handles, and provide useful diagnostics without exposing Flutter internals as the public contract.

## Acceptance criteria

- [x] `resolveFinder` supports exact visible `byText`.
- [x] `resolveFinder` supports semantic `byType` without treating it as Dart widget runtime type.
- [x] Combined `byText` and `byType` use AND semantics.
- [x] Offstage, inactive, zero-size, and known invisible targets do not match.
- [x] Zero, one, and multiple match cases are observable by Flutter Pilot's strict cardinality logic.
- [x] Match diagnostics include text, semantic type, action widget type when available, bounds when available, and an opaque Runtime Handle.
- [x] Widget tests cover visible matching, offstage exclusion, semantic type matching, AND combinations, and cardinality.

Verification:

- `dart format .`
- `dart analyze`
- `dart test`
- `flutter analyze` from `packages/pilot_runtime`
- `flutter test` from `packages/pilot_runtime`

## Blocked by

- 1. Scaffold pilot_runtime with protocol handshake

## 6. Implement byKey and byWidget Finder resolution

Status: completed in working tree.

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Extend Finder resolution and Scenario modeling with `byKey` and `byWidget`. `byKey` should match visible `ValueKey<String>` targets, `byWidget` should match exact Dart widget runtime type display names, and both should combine with existing Finder fields across the same visible target subtree.

## Acceptance criteria

- [x] Scenario model and parser accept single-string `byKey` and `byWidget` Finder fields.
- [x] `byKey` matches only `ValueKey<String>` values in v1.
- [x] `byWidget` matches exact Dart widget runtime type display names.
- [x] `byKey`, `byWidget`, `byText`, and semantic `byType` combine with AND semantics.
- [x] Wrapper-child subtree evidence can collapse to one Runtime Target match.
- [x] Match diagnostics distinguish `matchedWidgetType` and `actionWidgetType` when they differ.
- [x] Tests cover valid parsing, invalid non-string values, key matching, widget type matching, combined matching, and multiple-match failures.

Verification:

- `dart format .`
- `dart analyze`
- `dart test`
- `flutter analyze` from `packages/pilot_runtime`
- `flutter test` from `packages/pilot_runtime`

## Blocked by

- 5. Implement visible Finder resolution for byText and semantic byType

## 7. Implement tap action through PilotRuntimeAdapter

Status: completed in `168a7c9 feat(runtime): implement pilot runtime tap replay`.
Tap failure reporting was stabilized in
`37840d8 fix(runtime): return structured tap failures`.

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Implement tap replay through `pilot_runtime` and `PilotRuntimeAdapter`. A Scenario tap should resolve a unique Runtime Target match, pass its opaque Runtime Handle back to the runtime, prefer semantic tap when available, fall back to pointer center tap on calibrated platforms, and fail clearly when the target cannot be tapped.

## Acceptance criteria

- [x] `PilotRuntimeAdapter.performTap()` calls the `pilot_runtime` tap capability with the opaque Runtime Handle.
- [x] App-side tap prefers semantic tap actions when available.
- [x] App-side tap can fall back to pointer center tap on calibrated platforms.
- [x] Non-tappable matches produce a clear action failure.
- [x] Runner behavior still applies strict Finder cardinality before attempting the tap.
- [x] Tests cover adapter mapping, semantic tap, pointer fallback, non-tappable target failure, and Step failure reporting.

Verification:

- `dart analyze`
- `dart test`
- `flutter analyze` from `packages/pilot_runtime`
- `flutter test` from `packages/pilot_runtime`
- `flutter analyze` from `examples/smoke_app`
- Manual smoke runs from `examples/smoke_app`:
  - `FLUTTER_PILOT_RUNTIME=pilot_runtime dart run ~/.treehouse/flutter_pilot-b6aa0b/4/flutter_pilot/bin/flutter_pilot.dart test pilot/pilot_runtime_tap.yaml --target lib/pilot_runtime_tap_demo.dart`
  - `FLUTTER_PILOT_RUNTIME=pilot_runtime dart run ~/.treehouse/flutter_pilot-b6aa0b/4/flutter_pilot/bin/flutter_pilot.dart test pilot/pilot_runtime_non_tappable.yaml --target lib/pilot_runtime_tap_demo.dart`

## Blocked by

- 5. Implement visible Finder resolution for byText and semantic byType
- 6. Implement byKey and byWidget Finder resolution

## 8. Implement type action for editable text targets

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Implement text entry through `pilot_runtime` and `PilotRuntimeAdapter`. A Scenario type action should resolve a unique Runtime Target match, verify that the match is editable text, clear existing text deterministically, then enter the Scenario text one character at a time. Non-editable targets should fail clearly without simulating platform keyboard or IME input.

## Acceptance criteria

- [x] `PilotRuntimeAdapter.clearText()` calls the `pilot_runtime` clear-text capability with Runtime Handle.
- [x] `PilotRuntimeAdapter.enterText()` calls the `pilot_runtime` enter-text capability with Runtime Handle and one text fragment.
- [x] Editable text targets have existing text cleared before Scenario text is entered character by character.
- [x] Non-editable targets fail with a clear message.
- [x] The implementation does not simulate platform keyboard or IME input.
- [x] Tests cover adapter mapping, successful text entry, non-editable failure, and Step failure reporting.

## Blocked by

- 6. Implement byKey and byWidget Finder resolution

## 9. Implement scroll action with targeted and primary scrollables

Status: completed in `3792e7e feat(runtime): implement pilot runtime scroll replay`.
Follow-up coverage stabilizes keyed `ListView` plus `byType: scrollable`
resolution for the smoke Scenario.

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Implement scroll replay through `pilot_runtime` and `PilotRuntimeAdapter`. Targeted scroll should resolve a unique Runtime Target match and verify scroll capability. Untargeted scroll should resolve the primary scrollable. Both paths should use pointer drag gestures with Flutter logical pixel deltas and should fail rather than picking arbitrary targets.

## Acceptance criteria

- [x] Targeted scroll uses a unique Finder match and fails when the match is not scrollable.
- [x] Untargeted scroll resolves the primary scrollable.
- [x] Untargeted scroll fails clearly when the primary scrollable is missing or ambiguous.
- [x] Scroll uses pointer drag gestures with the Scenario `deltaX` and `deltaY` logical pixel semantics.
- [x] Semantic scroll actions are not used for v1.
- [x] Tests cover adapter mapping, targeted scroll, non-scrollable failure, primary scrollable success, and ambiguous primary failure.

Verification:

- `dart format .`
- `dart analyze`
- `dart test`
- `flutter analyze` from `packages/pilot_runtime`
- `flutter test` from `packages/pilot_runtime`
- `flutter analyze` from `examples/smoke_app`
- `dart run bin/flutter_pilot.dart validate examples/smoke_app/pilot/pilot_runtime_scroll.yaml`
- `dart run bin/flutter_pilot.dart validate examples/smoke_app/pilot/pilot_runtime_scroll_non_scrollable.yaml`

## Blocked by

- 6. Implement byKey and byWidget Finder resolution

## 10. Implement Flutter-layer screenshot capture

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Implement screenshot capture for `pilot_runtime` using a Flutter-layer or Flutter Inspector screenshot path. The result should map through `PilotRuntimeAdapter` into Flutter Pilot's existing screenshot artifact flow without using device screenshots.

## Acceptance criteria

- [ ] `pilot_runtime` exposes screenshot capture returning image bytes and MIME type.
- [ ] `PilotRuntimeAdapter.captureScreenshot()` maps the runtime result to Flutter Pilot `ScreenshotCapture`.
- [ ] The implementation uses a Flutter-layer or Inspector screenshot path, not a device screenshot.
- [ ] Screenshot capture failures map to Runtime Adapter operation failures.
- [ ] Tests cover success bytes/MIME mapping and failure mapping.

## Blocked by

- 3. Expose PilotRuntimeAdapter behind hidden runtime switch

## 11. Return not-implemented logs artifact without failing capture

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Keep Logs in the capture contract without implementing real log collection in `pilot_runtime` v1. A logs capture should return a structured not-implemented payload, write an artifact, and allow the capture Step to pass.

## Acceptance criteria

- [ ] `pilot_runtime` collect logs returns a structured not-implemented payload.
- [ ] `PilotRuntimeAdapter.collectLogs()` maps that payload to Flutter Pilot `LogsCapture`.
- [ ] A `logs: true` capture writes a logs artifact and does not fail because logs are not implemented.
- [ ] Tests cover payload shape, artifact writing, and capture Step success.

## Blocked by

- 4. Migrate structured UI capture from snapshot to widgetTree

## 12. Implement hot reload and hot restart client capabilities

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Add hot reload and hot restart capabilities to the `pilot_runtime` client using VM Service or Flutter runtime paths. These capabilities should not be Scenario Step actions and should not be app-side hook extensions.

## Acceptance criteria

- [ ] `pilot_runtime` client exposes hot reload and hot restart operations.
- [ ] The operations use VM Service or Flutter runtime capabilities rather than `ext.flutter_pilot.runtime.*` hook extensions.
- [ ] Success and failure responses are normalized into Flutter Pilot-owned result or error types.
- [ ] Tests cover successful hot reload, successful hot restart, and failure mapping using fake runtime responses.

## Blocked by

- 1. Scaffold pilot_runtime with protocol handshake

## 13. Add pilot_runtime smoke app and calibrated replacement checks

## Parent

https://github.com/drown0315/flutter_pilot/issues/110

## What to build

Create the live calibration slice that proves `pilot_runtime` is ready to replace `mcp_toolkit` later. The smoke app and calibration checks should run against macOS desktop and Android debug Runtime Targets and verify the required runtime capabilities end to end. This issue is HITL because it depends on real devices or simulators and human review of calibration results.

## Acceptance criteria

- [ ] A smoke target initializes `PilotRuntimeBinding` in debug mode.
- [ ] macOS desktop debug calibration verifies `byText`, semantic `byType`, `byKey`, `byWidget`, tap, type, targeted scroll, untargeted scroll, Screenshot, Widget Tree, hot reload, and hot restart.
- [ ] Android debug calibration verifies the same required capabilities where available.
- [ ] Calibration output records platform, Flutter SDK version, device target, commands, and observed results.
- [ ] The calibration result explicitly states that Web, profile, release, and iOS are not claimed by v1.
- [ ] `mcp_toolkit` remains the default runtime after this issue; replacement is not performed here.

## Blocked by

- 2. Capture normalized Widget Tree through pilot_runtime
- 7. Implement tap action through PilotRuntimeAdapter
- 8. Implement type action for editable text targets
- 9. Implement scroll action with targeted and primary scrollables
- 10. Implement Flutter-layer screenshot capture
- 11. Return not-implemented logs artifact without failing capture
- 12. Implement hot reload and hot restart client capabilities
