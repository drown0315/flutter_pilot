name: flutter_pilot
description: A CLI for replaying Flutter UI scenarios and collecting debugging artifacts.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

executables:
  flutter_pilot:
dependencies:
  args: ^2.7.0
  crypto: ^3.0.7
  path: ^1.9.1
  pilot_runtime_client:
    path: packages/pilot_runtime_client
  screen_recorder:
    path: packages/screen_recorder
  vm_service: ^15.2.0
  yaml: ^3.1.3
dev_dependencies:
  file_testkit: ^1.0.0
  lints: ^6.1.0
  test: ^1.31.1
