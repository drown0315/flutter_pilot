name: flutter_pilot
description: A CLI for replaying Flutter UI scenarios and collecting debugging artifacts.
version: 0.1.0

environment:
  sdk: ^3.9.0

executables:
  flutter_pilot:
dependencies:
  args: ^2.7.0
  path: ^1.9.1
  yaml: ^3.1.3
dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
