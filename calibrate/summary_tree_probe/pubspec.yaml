name: summary_tree_probe
description: Calibration CLI for Flutter Inspector summary widget tree output.
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  vm_service: 14.3.1
dev_dependencies:
  test: ^1.25.8
